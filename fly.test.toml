app = "burro-dos-concertos-test"
primary_region = "lhr"

[build]

[deploy]
  release_command = "sh -c 'pnpm exec prisma migrate deploy || echo \"Database already initialized\"'"

[env]
  NODE_ENV = "test"
  GROUP_ID = "-1003673049391"
  SUPER_ADMIN_ID = "144133589"

# BOT_TOKEN set via: fly secrets set BOT_TOKEN=xxx -a burro-dos-concertos-test

# Usage: fly deploy -a burro-dos-concertos-test -c fly.test.toml